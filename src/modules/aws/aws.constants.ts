/** US-12-12: OOS acknowledgement comment minimum length (trimmed). */
export const MIN_OOS_ACK_COMMENT_LENGTH = 20;

/** US-7-8: expired instrument/reagent acknowledgement comment minimum length (trimmed). */
export const MIN_EXPIRY_ACK_COMMENT_LENGTH = 10;
